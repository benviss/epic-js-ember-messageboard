
import { questionsDisplay } from 'messageboard/helpers/questions-display';
import { module, test } from 'qunit';

module('Unit | Helper | questions display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionsDisplay([42]);
  assert.ok(result);
});

